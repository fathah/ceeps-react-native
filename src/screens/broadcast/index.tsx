import { Text, View } from "react-native";
import BroadcastHeader from "./comps/Header";
import { SafeAreaView } from "react-native-safe-area-context";

const BroadCast = () => {
    return (
        <SafeAreaView>
           
            <BroadcastHeader />
               
                <Text >ff</Text>
       
        </SafeAreaView>
    );
}

export default BroadCast;